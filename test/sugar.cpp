#include "include/catch.hpp"

#include <sugar>


GIVEN("a Sugar is derived") {
  class CustomSugar : Sugar {
    //
  };

  WHEN("it is being serialized") {
    THEN("fields are outputted in a tuple") {
      //
    }
  }

  WHEN("it is being clustered") {
    THEN("the amount of Sugar must be defined") {
      // REQUIRE();
    }
    THEN("the Sugar must be distributable") {
      // REQUIRE(sugar.distribution !== null);
    }
    THEN("the correct amount of Sugar must be clustered accoringly") {
      }}

  WHEN("is is being distributed") {
    THEN("an amount of Sugar to distribute must be defined") {
      //
    }}
}

GIVEN("a SpatialPartition is being constructed") {
  
}

GIVEN("a SpatialPartition is constructed") {
  WHEN("") {
    THEN("") {
      
    }}

  WHEN("") {
    THEN("") {
      
    }}}

GIVEN("a Sugar dependency graph is being constructed") {
  WHEN("") {
    THEN("") {
      
    }}

  WHEN("") {
    THEN("") {
      
    }}
}

// GIVEN("") {
  
// }
