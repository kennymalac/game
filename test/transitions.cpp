#include "include/catch.hpp"

#include <transitions>


GIVEN("a Transition is being derived") {
  WHEN("") {
    THEN("") {
      
    }}

  WHEN("") {
    THEN("") {
      
    }}}


GIVEN("a TransitionMap is being defined") {
  WHEN("") {
    THEN("") {
      
    }}
}

GIVEN("a ActiveTransition is defined") {
  WHEN("state is modified in a Scene") {
    THEN("modified state is available in the next Scene") {
      
    }}
}

GIVEN("a PassiveTransition is defined") {
  WHEN("state is modified in a Scene") {
    THEN("the Scene errors") {
      
    }
  }
}
