~TerrainShader::TerrainShader (Grid& g) : (){
  // establish identity objects for shader accessors
  //this->tracePixels = glm::mat4x4
  // RAII - resource access is initialization
  }

// Stream
// Iteratable, memory storage

class Streamable {
public:
  Streamable () {
    
    }

private:
  istreambuf_iterator<char> i(send);
  ostreambuf_iterator<char> o(receive);}

ostream&operator<<(ostream& sm, const Streamable& s) {
  sm << s->receive()}

ostream&operator>>(istream& sm, Streamable& s) {
  sm >> s->send();
  return sm;}

Streamable ShaderProgram::receive() {
  };

// Plurality of Stream Interfaces
//struct Operator {
//  }


//overloadable, default object creation behavior
~Vao::Vao ();

void Vao::populate() {
  
  }

